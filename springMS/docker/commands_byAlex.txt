docker build --file=DockerFile.accMiService --tag=account-micro-service:latest --rm=true .   // az exec jar-t kell megadni neki, amit a pom-ban beállítottunk az exec configgal
docker run --name=account-micro-service --publish=2222:2222 account-micro-service:latest
docker run -d --name=account-micro-service --publish=2222:2222 account-micro-service:latest  // -d valamit csinál? detached
docker stop account-micro-service
docker inspect account-micro-service
docker rm docker_id                                                                         // sok szám és betű ha conflict error van
docker image ls
docker container ls -a

docker build --file=DockerFile.eureka --tag=eureka:latest --rm=true .
docker run -d --name=eureka --publish=1111:1111 eureka:latest

docker exec -it account-micro-service bash

docker-compose up --build -d
docker-compose down


docker logs docker_id


!!
docker-compose up --build -d
